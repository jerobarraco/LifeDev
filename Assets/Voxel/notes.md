# Dimensions
Scales.
beware the scale is applied ON IMPORT POPUP DIALOG

* regular:
>> ! when imported scaled down to 0.05 <<
cube size 5 cm

* small:
1:10 of regular
imported scaled down to 0.005
cube size approx 5mm = 0.5 cm

* big:
10:1 regular. e.g. buildings
todo test
scaled to 0.5
approx size 50cm = .5m



# palette
preview in srgb should be enabled. when using the png palette.
copy the color into the srgb box.
the preview window will mismatch everything probably, don't worry. pick whatever looks ok on the preview don't worry about the picker.
but leave picker colors correct.

# characters
* the characters have their pivots changed on editor. beware.


# screenshot for interacts
in goxel
* go to >view<
  ** set "hide box"
  ** unset "unlit"

position:
* try to keep a sense of scale. but remember old items don't follow this rule. so don't over do it.
* * i.e. fill the screen relatively to how big the object is.
* some items are inclined to the left (as in viewed from the left)
* * flat items are viewed a bit from the top (paper, card, scissor)

* go to >export<
* set custom size : 512x512
* no transparent
* export
  ** name with _T postfix or unreal might confuse with the mesh

upon import
Textures for icons

group : UI

/// nopes: max texture size : 256

